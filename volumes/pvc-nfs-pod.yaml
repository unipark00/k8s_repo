apiVersion: v1
kind: Pod
metadata:
  name: pvc-nfs-pod
spec:
  containers:
    - name: myfrontend
      image: nginx
      volumeMounts:
        - name: pvc-vol
          mountPath: "/var/www/html"
  volumes:
    - name: pvc-vol
      persistentVolumeClaim:
        claimName: pvc-nfs
